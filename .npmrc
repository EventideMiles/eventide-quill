tag-version-prefix=""
# The allowScripts/denyScripts fields in package.json (npm 11.16+) are only
# honored by npm >= 11.16.0; fail the install instead of silently running or
# skipping lifecycle scripts under an older npm. Pinned via packageManager.
engine-strict=true